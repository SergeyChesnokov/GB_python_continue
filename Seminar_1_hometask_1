# //////////////   ДЗ №1   /////////////////

print('\n---ПРОВЕРЬ ТРЕУГОЛЬНИК---\n\nПрограмма определяет возможность существования треугольника и его тип по трём сторонам.')
print('Введите последовательно три стороны a, b и с. Отрицательные длины берутся по модулю.\n')
print('Для выхода из программы введите "0" вместо длины одной из сторон.\n')


while True:

#   ===   Блок ввода данных:   ===

    flag_a = True
    while flag_a == True:
        a = input('\n--->   Введите сторону треугольника a или 0 для выхода:  a =  ')
        try:
            a = abs(float(a))
            flag_a = False
        except:
            print('\n!!! Программа принимает только числа!!!')
        if a == 0.0:
            exit('\nДо новых встреч\n')

    flag_b = True
    while flag_b == True:
        b = input('\n--->   Введите сторону треугольника b или 0 для выхода:  b =  ')
        try:
            b = abs(float(b))
            flag_b = False
        except:
            print('\n!!! Программа принимает только числа!!!')
        if b == 0.0:
            exit('\nКуда же Вы? Всё так хорошо начиналось...\n')

    flag_c = True
    while flag_c == True:
        c = input('\n--->   Введите сторону треугольника c или 0 для выхода:  c =  ')
        try:
            c = abs(float(c))
            flag_c = False
        except:
            print('\n!!! Программа принимает только числа!!!')
    if c == 0:
        exit('\nНу нормально же общались? Ладно, в другой раз посчитаем...\n')

#   ===   Блок решения:   ===

    if (a + b > c) & (b + c > a) & (c + a > b):
        print('\nТакой треугольник и впрямь СУЩЕСТВУЕТ')

        if a == b == c:
            print('Его называют РАВНОСТОРОННИМ или правильным')
        elif (a == b) | (b == c) | (c == a):
            print('Это РАВНОБЕДРЕННЫЙ треугольник')
        elif (a**2 + b**2 == c**2) | (b**2 + c**2 == a**2) | (c**2 + a**2 == b**2):
            print('Этот треугольник -- ПРЯМОУГОЛЬНЫЙ')
        else:
            print('Это произвольный разносторонний треугольник')
    else:
        print('\nНет таких, не сойдутся стороны')       
    print('\n\nПроверим следующий?')


